# Harness Adapters

Adapters map the portable core to an agent runtime. They may name paths, tools, hooks, and command syntax; generic skills must not. Start with `generic.md`, then apply the adapter matching the active harness when available.
