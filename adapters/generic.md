# Generic Adapter

Load `docs/portable-core.md` and expose only capabilities the runtime actually provides. Load skills on demand using their metadata. Do not assume shell access, browser automation, networking, subagents, hooks, or a writable workspace.
